services:
  likevercel:
    build: .
    image: likevercel-backend:latest
    container_name: likevercel-app
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./backend/prisma:/app/prisma
    restart: unless-stopped

networks:
  default:
    driver: bridge
